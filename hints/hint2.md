Once you add a catch block,
think about two separate audiences:

1. You, the developer
2. The user

For yourself, logging the raw error
is enough.

For the user, you should render
a short, friendly message into the UI.

Error messages should appear in the same
place as AI responses: the output container.
